import 'package:flutter/material.dart';

class ImageStackAnimation extends StatefulWidget {
  const ImageStackAnimation({super.key});

  @override
  State<ImageStackAnimation> createState() => _ImageStackAnimationState();
}

class _ImageStackAnimationState extends State<ImageStackAnimation> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
